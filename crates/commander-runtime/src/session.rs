// Session will compose all subsystems (registry, permissions, hooks, adapter, transcript)
// into a single entry point for agent execution. For now, the agent_loop module provides
// the core functionality directly. Session will be fleshed out when the CLI needs it.
